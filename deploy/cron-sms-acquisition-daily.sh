#!/usr/bin/env bash
set -euo pipefail
# OBSOLÈTE — le cron SMS ne prépare plus de lots.
# Utiliser deploy/cron-sms-send-8h.sh (lun–sam 8h Paris).
echo "Ignoré : prepare n'est plus une tâche cron SMS."
exit 0
